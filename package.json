{
  "name": "css-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "check": "eleventy && npx -y html-validate \"_site/**/*.html\" && npx -y csstree-validator src/style.css",
    "deploy": "npx -y wrangler pages deploy _site --project-name css-html-t --branch main --commit-dirty=true"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nitrogoboom/css-html-t.git"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@11ty/eleventy-plugin-rss": "^3.0.0"
  }
}
